# Esempio variabili Vite per il frontend.
# Copia in `.env.production` prima del build di prod (è gitignored).
# In dev non serve niente: api.js usa il fallback http://localhost:8000.

VITE_API_URL=https://hub.parametricomparison.unimore.it
